<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      The platform splash, API 31+. The background tracks the app's own ground
      (values-night swaps it), and the icon slot is left blank — see
      drawable/splash_blank.xml.
    -->
    <style name="Theme.OmaykanRider.Starting" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowSplashScreenBackground">@color/window_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_blank</item>
    </style>
</resources>
