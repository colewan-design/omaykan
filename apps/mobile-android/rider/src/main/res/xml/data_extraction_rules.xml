<?xml version="1.0" encoding="utf-8"?>
<!--
  Nothing here leaves the device. The token in EncryptedSharedPreferences is a
  personal credential for a rider's own account — restored onto a stranger's
  phone it would hand them a live job board, the addresses on it, and the
  ability to mark somebody else's delivery done.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
