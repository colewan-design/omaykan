<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext is permitted to the emulator's host loopback and nothing else, so a
  debug build can talk to `php artisan serve` on the laptop without opening the
  app up to plain HTTP generally. Release traffic is https://omaykan.com.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
