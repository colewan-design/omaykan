{
  "name": "pos-workspace",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace web",
    "build:web": "npm run build --workspace web"
  },
  "devDependencies": {
    "@types/node": "^24.12.3",
    "playwright": "^1.61.1",
    "typescript": "~6.0.2"
  }
}
