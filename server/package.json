{
  "name": "colepos-api",
  "private": true,
  "version": "0.1.0",
  "description": "Self-hosted runner for the api/ handlers (same source Vercel deploys).",
  "type": "commonjs",
  "main": "dist/server/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server/main.js"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0"
  },
  "devDependencies": {
    "@types/node": "^24.12.3",
    "typescript": "~6.0.2"
  }
}
